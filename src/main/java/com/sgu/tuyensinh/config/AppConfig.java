package com.sgu.tuyensinh.config;

import org.springframework.context.annotation.Configuration;

/**
 * Cấu hình chung của ứng dụng.
 *
 * Hiện tại class này đang "trống" vì dự án mới khởi tạo.
 * Khi cần cấu hình thêm (ví dụ CORS, message converter, formatter...), nhóm sẽ đặt vào đây.
 *
 * Lưu ý: Không cần làm phức tạp. Cứ thêm từng cấu hình nhỏ khi phát sinh nhu cầu.
 */
@Configuration
public class AppConfig {
}
